# Kasm Workspaces Configuration

# Base Docker path for persistent data
DOCKER_BASE_PATH=/docker

# Traefik configuration
TRAEFIK_BASE_DOMAIN=example.com

# Kasm specific configuration
# Port for Kasm web interface (must match both internal and external)
KASM_PORT=8443

# Setup wizard port (only needed during initial installation)
KASM_SETUP_PORT=3000

# Optional: DockerHub credentials for pulling private images
DOCKER_HUB_USERNAME=
DOCKER_HUB_PASSWORD=

# Optional: Docker MTU setting (default: 1500)
DOCKER_MTU=1500
