# Zerobyte Backup Automation Environment Variables
# Copy this file to .env and update with your values

# User and Group IDs for file permissions
PUID=1000
PGID=1000

# Timezone (must match your location for accurate backup scheduling)
TZ=America/Los_Angeles

# Base path for Docker volumes
DOCKER_BASE_PATH=/docker

# Base domain for Traefik routing
TRAEFIK_BASE_DOMAIN=example.com

# Required: Secret for encrypting database data (v0.23+)
# Generate with: openssl rand -hex 32
APP_SECRET=your_64_character_hex_secret_here

# Required: Base URL for accessing Zerobyte (v0.24+)
# Must match your primary access point (including https:// or http://)
BASE_URL=https://zerobyte.example.com

# Optional Zerobyte Configuration (uncomment and configure if needed)
# These have sensible defaults in the docker-compose.yml
# PORT=4096
# RESTIC_HOSTNAME=zerobyte
# LOG_LEVEL=info
# SERVER_IDLE_TIMEOUT=60
# TRUSTED_ORIGINS=
