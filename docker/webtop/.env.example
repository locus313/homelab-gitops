# Webtop Environment Variables
# Copy this file to .env and modify the values as needed

# User and Group IDs for file permissions
PUID=1000
PGID=1000

# Timezone
TZ=America/Los_Angeles

# Docker base path for volume mounts
DOCKER_BASE_PATH=/docker

# Traefik domain configuration
TRAEFIK_BASE_DOMAIN=yourdomain.com

# Webtop image tag - Choose your preferred desktop environment:
# fedora-i3    - i3 window manager (minimal, keyboard-driven)
# fedora-kde   - KDE Plasma desktop (feature-rich, modern)
# fedora-mate  - MATE desktop (traditional, lightweight)
# fedora-xfce  - XFCE desktop (fast, customizable)
# latest       - XFCE Alpine (default, most lightweight)
WEBTOP_TAG=fedora-kde

# Optional: Specific GPU device for DRI3 acceleration (leave empty for auto-detection)
# Example: /dev/dri/renderD128 for a specific GPU
# Only needed if you have multiple GPUs and want to use a specific one
DRINODE=

# Basic Authentication (optional but recommended)
# Leave PASSWORD empty to disable authentication (not recommended for internet exposure)
CUSTOM_USER=admin
PASSWORD=your-secure-password-here

# Optional: Subfolder for reverse proxy setup (leave empty if not using)
SUBFOLDER=

# Optional: Custom title for the desktop environment
TITLE=Webtop Desktop
